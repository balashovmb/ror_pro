= form_tag search_path, method: :get do |f|
  = search_field_tag :query, params[:query], placeholder: "Search"
  = select_tag 'object', options_for_select(Search::OBJECTS, params[:object]) 
  = submit_tag 'Find'