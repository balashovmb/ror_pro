var target = '<%= "#{@commentable.class.name}-#{@commentable.id}-comments" %>';
$('#' + target).append("<%= j render 'comments/form', commentable: @commentable, comment: @comment%> ");

$form.on('click', '.cancel-comment', function(e) {
  e.preventDefault();
  $form.find('.row').remove();
  $trigger.show();
});