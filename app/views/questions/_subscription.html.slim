- if @subscription.persisted?
    = link_to 'Unsubscribe', subscription_path(@subscription), remote: true, method: :delete,  class: 'unsubscribe-link'
- else
    = link_to 'Subscribe', question_subscriptions_path(@question), remote: true, method: :post,  class: 'subscribe-link'
